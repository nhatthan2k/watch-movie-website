export const AUTH = (state) => state.auth;

export const GENRE = (state) => state.genre;

export const MOVIE = (state) => state.movie;

export const SEASON = (state) => state.season;

export const EPISODE = (state) => state.episode;

export const USER = (state) => state.user;

export const USERS_ADMIN = (state) => state.userAdmin;

// delete
export const SIZE = (state) => state.size;

export const ORDER_USER = (state) => state.orderUser;

export const ORDER_ADMIN = (state) => state.orderAdmin;
